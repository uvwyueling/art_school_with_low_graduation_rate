# 数据字典 (Data Dictionary) - 艺术学院服装专业低毕业率原因分析

## 1. 原始数据实体 (Raw Data Entity)
**表名：** `[如：trip_data_raw]`
**说明：** 业务系统导出的最底层原始记录，未做任何加工。

| 字段名称 | 数据类型 | 业务含义 | 缺失值/异常值状态声明 |
| :--- | :--- | :--- | :--- |
| [字段A] | [如：STRING] | [定义，如：骑行唯一ID] | [如：主键，无缺失] |
| [字段B] | [如：TIMESTAMP] | [定义，如：系统生成的结束时间] | [如：无缺失] |
| [字段C] | [如：FLOAT] | [定义，如：起点站纬度] | [如：缺失率约 20%，因无桩业务特性导致] |

---

## 2. 清洗后分析宽表 (Processed Analytical Table)
**表名：** `[如：clean_trip_data]`
**说明：** 经过异常值剔除并衍生了业务特征的主力分析表。
**前置处理逻辑：**
* 剔除了 `[条件A]` 的噪音数据。
* 过滤了时长 `<60` 秒的无效记录。

| 字段名称 | 数据类型 | 业务含义 | 派生计算逻辑说明 (Derivation Logic) |
| :--- | :--- | :--- | :--- |
| [基础字段保留] | ... | ... | (继承自原表) |
| [派生字段A] | [如：INT64] | [如：骑行时长（秒）] | `TIMESTAMP_DIFF(ended_at, started_at, SECOND)` |
| [派生字段B] | [如：FLOAT] | [如：网格化纬度] | `ROUND(start_lat, 2)` (统一将空间分辨率降维至 1km 网格) |